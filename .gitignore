
CK3-Modding.code-workspace
docs/Crusader Kings III.code-workspace

# ck3-tiger lint baseline: machine-bound (absolute local paths) and regenerates on
# every game patch, so it stays local - run the lint, don't commit the snapshot.
.ck3modding/

# Generated diagnostic metadata contains local source paths.
/DI_grid_manifest.json
